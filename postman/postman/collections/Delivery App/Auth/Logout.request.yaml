$kind: http-request
url: "{{base_url}}/api/auth/logout"
method: POST
headers:
  Authorization: Bearer {{access_token}}
body:
  type: json
  content: "{\r

    \  \"refreshToken\": \"{{refresh_token}}\"\r

    }"
order: 8000
